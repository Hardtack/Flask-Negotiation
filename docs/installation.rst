Install Flask-Negotiation
=========================

Install with pip
----------------

You can install it using pip::  

    pip install Flask-Negotiation

Install with distutils
----------------------
Install with distutils::  

    python setup.py install
